```
php generate-queue.php -e default -c default
php execute.php -c default // based only on the queue
```

```
sudo apt-get install texlive-latex-base
sudo apt-get install texlive-pictures
sudo apt-get install texlive-lang-polish
sudo apt-get install texlive-full // requires 4GB
```
